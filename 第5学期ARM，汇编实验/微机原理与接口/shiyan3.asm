DATA    SEGMENT
   X    DB 1
   Y    DW 1
   Z    DW 1
        RESULT DW 2 DUP(?)
DATA    ENDS
CODE    SEGMENT
        ASSUME CS:CODE,DS:DATA
START:  MOV AX,DATA
	MOV DS,AX
	MOV AX,4
	IMUL Y
	MOV CX,AX
	MOV BX,DX
	MOV AL,X
	CBW
	CWD
	ADD AX,CX
	ADC DX,BX
	IDIV Z
	MOV RESULT,AX
	MOV RESULT+2,DX
	MOV AH,4CH
	INT 21H
CODE    ENDS
        END START